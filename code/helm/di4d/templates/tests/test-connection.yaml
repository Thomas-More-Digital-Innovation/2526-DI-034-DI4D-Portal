apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "di4d.fullname" . }}-test-connection"
  labels:
    {{- include "di4d.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "di4d.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
