{% extends "structure/structure_public.jinja" %}

{% block title %}{{ talk.title }} - Tech Talk{% endblock %}

{% block content %}
<div class="bg-[var(--blue_di4d)] min-h-screen p-8">
    <div class="max-w-6xl mx-auto w-full">
        <a class="mb-6 inline-flex items-center px-4 py-2 rounded-full bg-[var(--orange_di4d)] text-white font-semibold hover:bg-orange-600 transition" href="{{ url('tech_talks') }}">
            <i class="fa-solid fa-arrow-left mr-2"></i> Back to techtalks
        </a>
        <h1 class="text-3xl lg:text-4xl font-bold text-white text-center mb-8">{{ talk.title }}</h1>

        {% include 'components/techtalk_detail_htmx.jinja' %}
    </div>
</div>
{% endblock %}
