{% extends "structure/structure_public.jinja" %}

{% block title %}{{ talk.title }} - Tech Talk{% endblock %}

{% block content %}
<div class="bg-[var(--blue_di4d)] min-h-screen p-8">
    <div class="max-w-6xl mx-auto w-full">
        <div class="relative flex items-center mb-8">
            <a class="absolute left-0 inline-flex items-center px-4 py-2 rounded-full bg-white text-[var(--blue_di4d)] font-semibold hover:bg-gray-100 transition" href="{{ url('tech_talks') }}">
                <i class="fa-solid fa-arrow-left mr-2"></i> Back
            </a>
            <h1 class="text-3xl lg:text-4xl font-bold text-white text-center w-full">{{ talk.title }}</h1>
        </div>

        {% include 'components/techtalk_detail_htmx.jinja' %}
    </div>
</div>
{% endblock %}
