{% extends "structure/structure_private.jinja" %}

{% block title %}{{ talk.title }} - Tech Talk{% endblock %}

{% block content %}
<div class="bg-white min-h-screen p-8">
    <div class="max-w-6xl mx-auto w-full">
        <div class="relative flex items-center mb-8">
            <a class="absolute left-0 inline-flex items-center px-4 py-2 rounded-md btn_primary_orange text-white font-semibold hover:opacity-90 transition" href="{{ url('tech_talks') }}">
                <i class="fa-solid fa-arrow-left mr-2"></i> Back
            </a>
            <h1 class="text-3xl lg:text-4xl font-bold text-black text-center w-full">{{ talk.title }}</h1>
        </div>

        {% include 'components/techtalk_detail_private_htmx.jinja' %}
    </div>
</div>
{% endblock %}
